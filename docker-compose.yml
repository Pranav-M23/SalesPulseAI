version: "3.9"

services:
  salespulse-api:
    build: .
    container_name: salespulse-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./salespulse.db:/app/salespulse.db
    restart: unless-stopped
